#[cfg(test)]
mod tests {

    /// Sends a valid request to the API and checks if it responds correctly
    #[test]
    fn http_send_success() {
         assert!(false);
    }

    /// Sends an invalid request and checks if we recieve a 400 error.
    #[test]
    fn http_send_error() {
        assert!(false);
    }

}
